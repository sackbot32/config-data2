server:
 port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sackbot32/config-data2.git
          searchPaths: config-data  
security:
 user:
 name: root
 password: passedword
          